%h1 All articles

- @articles.each do |article|
	%h2= link_to article.title, article
	%p
		Submited at
		= article.created_at.strftime('%b %d, %Y')
	%p= truncate(article.content, length: 5)

= link_to "Create new article", new_article_path